# เหตุผลที่เลือก Architecture แบบนี้สำหรับ V1

1. หลีกเลี่ยงปัญหา IAM ใน Learner Lab เนื่องจาก CloudFront ติดปัญ การตัด CloudFront ออกใน V1 ทำให้ทีมไม่ต้องเสียเวลาแก้ปัญหาตั้งแต่ยังไม่จำเป็นต้องใช้ HTTPS/CDN จริงจัง

2. ยังต่อยอดได้ในเวอร์ชันหลัง  โครงสร้างข้อมูลใน S3 ที่วางไว้ ไม่เปลี่ยน จะเพิ่ม CloudFront ครอบด้านหน้าทีหลังตอนที่มี requirement ชัดเจนกว่านี้ ก็ทำได้โดยไม่ต้องรื้อของเดิม